/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ["static123.com"], // Add your domain here
    },
};

export default nextConfig;
